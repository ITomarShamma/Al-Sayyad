# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environment
.venv/
venv/

# Django
db.sqlite3
db.sqlite3-journal
/staticfiles/
/media/

# Backups (manage.py backup)
/backups/

# Secrets / environment
.env

# Compiled translations (regenerated with compilemessages)
# ملاحظة: ملفات ‎.mo (الترجمات المُجمَّعة) تُرفع عمداً — لا خط نشر يجمعها بعد،
# ورفعها يضمن اشتغال الإنجليزية من أي نسخة جديدة بدون خطوة إضافية.

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local tooling (machine-specific absolute paths)
.claude/
