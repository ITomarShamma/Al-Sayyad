# نظام تصميم الصَّيَّاد (DESIGN)

> دليل الهوية البصرية وتجربة الاستخدام: من أين تأتي كل قيمة، وكيف تبني
> واجهة جديدة تشبه الموقع. ترافق `architecture.md` · العرض الحي لكل
> المكوّنات وحالاتها: `/styleguide/` · آخر تحديث: 2026-07-10 (بعد M29).

## الفلسفة: خمس قواعد ذهبية

صُمّم المتجر لزبون قد يشتري أونلاين **لأول مرة بحياته**. كل قرار بصري
يخدم هذا:

1. **كل شاشة = مهمة واحدة.** صفحة إتمام الطلب لا تسوّق، وصفحة المنتج لا
   تشتّت. إن احتجت شرح الشاشة بجملتين فهي شاشتان.
2. **Tokens ثم مكوّنات ثم صفحات.** أي قيمة بصرية من `tokens.css` حصراً؛
   أي قطعة UI تتكرر تصير مكوّناً في `templates/components/` يُستدعى
   بـ`{% include ... only %}` ويُعرض بحالاته في `/styleguide/`.
3. **ما في أيقونة بدون كلمة.** 🛒 وحدها لغز لمن لا يعرف الأعراف — «🛒 السلة»
   واضحة للجميع.
4. **محايد الاتجاه.** خصائص CSS منطقية فقط (`inline-start` لا `left`) —
   الواجهة تنقلب تلقائياً بين العربية RTL والإنجليزية LTR.
5. **أظهر الثقة والحالة دائماً.** طرق الدفع بالفوتر، حالة الطلب خط زمني،
   كل فعل يعقبه تأكيد (توست/رسالة)، وكل خطأ يقال بصوت عالٍ وبالعربية.

## الألوان

**المصدر الوحيد:** `static/css/tokens.css`. لا يُكتب hex خارج هذا الملف.

### ألوان البراند

| Token | القيمة | الدور |
|---|---|---|
| `--color-deep-sea` | `#0B4A41` | الرأس (هيدر الموقع واللوحة)، عناوين البطاقات، النص المالي |
| `--color-tide` | `#0E6B5E` | **الأساسي**: أزرار، روابط تفاعلية، الحالة النشطة |
| `--color-gold` | `#D99A2B` | **التمييز باقتصاد**: CTA الرئيسي، عدّاد السلة، علامات المقاطع، النجوم |
| `--color-sea-foam` | `#E4F1ED` | خلفيات هادئة: صور المنتجات، الاختيار، hover |

### المحايدة

| Token | القيمة | الدور |
|---|---|---|
| `--color-ink` | `#16261F` | النص الأساسي |
| `--color-slate` | `#5B6B64` | نص ثانوي/تلميحات |
| `--color-line` | `#E6E1D3` | حدود وفواصل |
| `--color-paper` | `#FAF8F2` | خلفية الصفحة (دافئة، ليست أبيض ساطعاً) |

### الدلالية + خلفياتها (tints)

لكل دلالة ثلاثية: لون قوي + خلفية فاتحة + نص داكن يُقرأ فوقها —
`--color-success/error/warning/info` مع `--tint-*` و`--on-tint-*`.
تُستعمل معاً دائماً (شارة، رسالة نظام، تنبيه فورم) — لا تُفرد الخلفية بلا
نصّها المرافق. ألوان الدفع: `--color-shamcash` `#1FA37A` و`--color-cod` `#7A5B2A`.

### أدوار لا أسماء

الواجهة تستعمل **الأدوار**: `--bg`, `--surface`, `--text`, `--text-muted`,
`--border`, `--primary`, `--primary-strong`, `--accent`, `--on-primary`.
لو تغيّرت الهوية غداً يتغيّر تعريف الدور بسطر — لا مطاردة ألوان بالملفات.

### قرارات لونية مقصودة (M29)

- **الهيدر Deep Sea لا Tide:** الغامق إطار راسخ يترك Tide لما يُنقر.
  نفس رأس لوحة التحكم — هوية واحدة بكل مكان.
- **الذهبي بالقطّارة:** كثرة الذهبي تقتل قيمته؛ هو للفعل الأهم بالشاشة.
- **سعر التخفيض أحمر** (`product-card__price--sale`) — يُقرأ فرصةً،
  والسعر القديم مشطوب بجانبه رمادياً.

## الطباعة

الخط: **IBM Plex Sans Arabic** مستضاف محلياً (`fonts.css` — لا CDN).

| Token | الحجم | الاستخدام |
|---|---|---|
| `--fs-display` | 44px | عنوان الـhero على الشاشات الكبيرة فقط |
| `--fs-h1` | 32px | عنوان الصفحة (واحد لكل صفحة) |
| `--fs-h2` | 24px | عناوين المقاطع (مع علامة ذهبية جانبية) |
| `--fs-h3` | 18px | عناوين فرعية وبطاقات |
| `--fs-body` | 16px | النص الأساسي — **لا نص تحت 13px أبداً** |
| `--fs-caption` | 13px | تلميحات وتواريخ وشروح |

أوزان: 400 نص · 500 تسميات · 600 عناوين فرعية وأزرار · 700 عناوين وأسعار.
الأسطر: `--lh-base` 1.75 للنص العربي (مريح للقراءة) و`--lh-tight` 1.3 للعناوين.
**ممنوع `letter-spacing` على العربية** — يكسر اتصال الحروف.
**الأرقام المالية تُنسَّق بالسيرفر** (`f"{...:,.0f}"`) — القالب تحت اللغة
العربية يطبع 4.0 كـ«٤٫٠» (درس M24).

## المسافات والأشكال والعمق

- **المسافات:** سلّم أساسه 4px — `--space-1..8` (4/8/12/16/24/32/48).
  لا قيم حرة؛ القرب البصري = علاقة منطقية.
- **الزوايا:** `--radius-sm` 6 (مدخلات صغيرة) · `--radius-md` 12 (بطاقات
  وحقول) · `--radius-lg` 20 (hero ومعرض الصور) · `--radius-pill` (أزرار وشارات).
- **الظلال — ثلاث طبقات عمق، كل ظل طبقتان** (قريبة حادة + بعيدة ناعمة):
  `--shadow-sm` للبطاقات الساكنة · `--shadow-md` للبطاقات المرفوعة hover
  والقوائم · `--shadow-lg` لأعلى طبقة (اقتراحات البحث، التوست).
- **الحركة:** `--transition-fast` 0.15s (ألوان/حدود) و`--transition-base`
  0.25s (رفعة/تكبير). `prefers-reduced-motion` يوقف كل حركة — دائماً.

## البنية (Layout)

- الحاوية `--container-max` 1200px، النصوص والفورمات `--content-narrow` 720px.
- نقطة تحوّل واحدة: **768px**. تحتها: بحث بسطر كامل + شريط تنقّل سفلي ثابت
  (بأهداف نقر `--tap-min` 48px). فوقها: تنقّل علوي بالهيدر ولا شريط سفلي.
- الهيدر لاصق (sticky) Deep Sea؛ الشريط السفلي أبيض مموّه (blur) بظل علوي.
- الفوتر ثلاثة أعمدة على الشاشات الكبيرة (الهوية / روابط / طرق الدفع)
  وعمود واحد ممركز على الموبايل، وتحته سطر الحقوق ومبدّل اللغة.
- شبكة المنتجات: `auto-fill minmax(150px)` موبايل / `minmax(210px)` فوق 768.

## المكوّنات (الكلاسات في components.css، القوالب في templates/components/)

| المكوّن | الكلاس | ملاحظات الحالة |
|---|---|---|
| زر | `.btn` + `--primary/--accent/--secondary/--block` | hover يغمق، الضغط ينزل 1px، تعطيل 55%، تحميل `.is-loading` بدوّارة |
| شارة | `.badge` + `--success/--warning/--error/--info/--cod/--shamcash` | ثلاثية اللون الدلالية دائماً |
| حقل | `.field` (+`__label/__input/__msg`) | تركيز: حد Tide + حلقة `--focus-ring`؛ خطأ: `--error` |
| بطاقة | `.card` (+`--link`) | `--link` ترتفع 2px مع `--shadow-md` عند hover |
| بطاقة منتج | `.product-card` | ترتفع 3px، الصورة تتكبّر 5%، الاسم سطران مقصوصان (تساوي الصفوف)، شارة `sale-flag` |
| شريحة | `.chip` | روابط تصنيفات بهدف نقر 44px |
| عدّاد سلة | `.nav-badge` (+`--float`) | ذهبي فوق الأيقونة، يتحدّث حيّاً (HTMX OOB) |
| سطر سلة | `.cart-item` + `.qty` | صورة 72px، عدّاد كمية بأزرار 40px |
| رسائل النظام | `.messages__item--*` | تظهر مرة بعد الفعل |
| توست | `.toast` | يظهر ويختفي بأنيميشن 3.5s بلا JS، `aria-live` |
| خيار دفع | `.pay-option` | راديو ببطاقة كاملة؛ المختار حدّ مضاعف + Sea Foam |
| خط زمن الطلب | `.timeline` | نقاط متصلة؛ الملوَّن «وصلنا له»، الحالي بهالة |
| اقتراحات البحث | `.search-suggest` | تحت الحقل مباشرة، `--shadow-lg` |
| حالة فارغة | `.empty` | أيقونة بدائرة Sea Foam + جملةودّية + فعل مقترح |
| مسار تنقّل | `.breadcrumbs` | بكل صفحات الكاتالوج |
| ترقيم | `.pagination` | يحفظ باراميترات الفرز والفلترة |

**إضافة مكوّن جديد:** قيمه من tokens فقط ← جزئية في `templates/components/`
تُستدعى بـ`only` ← أضفه بحالاته إلى `/styleguide/` ← استعمله.

## أنماط التفاعل

- **الاستجابة قبل النقر:** كل قابل للنقر له hover ملموس (رفعة/لون/خلفية).
- **الضغطة:** الأزرار تنزل 1px عند `:active`.
- **التركيز:** حلقة `:focus-visible` موحّدة (Tide) بكل الموقع — تظهر
  للكيبورد فقط لا للفأرة. داخل الهيدر الغامق: حقل البحث يتوهّج بحد ذهبي.
- **التأكيد:** إضافة للسلة = توست + تحديث عدّادين حيّين؛ حفظ = رسالة نجاح.
- **التحسين التدريجي:** كل شيء يعمل بلا JavaScript؛ HTMX يحسّن فقط.

## RTL والترجمة

- عربي أولاً؛ `dir` يتبدّل مع اللغة والـCSS المنطقي يقلب التخطيط وحده.
- كل نص جديد عبر `{% translate %}` ثم سير العمل:
  `makemessages` ← `scripts/translate_en.py` (يفشل معدِّداً الناقص) ←
  `compilemessages` (التفصيل في `development-log.md`).
- الأرقام الهندية (٠٩…) تُطبَّع أينما أدخلها المستخدم.

## الوصولية (Accessibility)

- أهداف نقر ≥ 48px (`--tap-min`)، نص ≥ 13px.
- كلمة مع كل أيقونة؛ `aria-label` للمناطق؛ `alt` لكل صورة منتج.
- `:focus-visible` واضح، `::selection` بهوية العلامة.
- `prefers-reduced-motion` يجمّد الحركة كلها.
- التباين: Ink على Paper وDeep Sea على أبيض يتجاوزان AA.

## لوحة التحكم

نفس الهوية عبر متغيّرات Django admin نفسها — `static/css/admin-theme.css` +
`templates/admin/base_site.html`: رأس Deep Sea، أزرار Tide، أدوات ذهبية،
الوضع الفاتح مثبَّت، وخط المتجر نفسه. لوحة المبيعات (`/admin/dashboard/`)
تتبع نفس النظام (بطاقات، مخطط أعمدة CSS، ألوان الحالات الدلالية).

## الأصول

- **اللوغو:** علامة «ص» مؤقتة (`brand__mark`) — عند وصول لوغو Figma:
  استبدل العلامة بالقوالب + أعد توليد أيقونات الـPWA
  (`python scripts/make_pwa_icons.py`) بنفس الأسماء.
- **الصور:** مصغّرات 480px JPEG تُولَّد تلقائياً عند الحفظ (M14).
- **الأيقونات:** إيموجي نظام مقصودة (بلا مكتبة أيقونات) — مفهومة ومجانية
  وتعمل بلا أصول إضافية، ودائماً بجانبها كلمة.
